@page "/schedule"

<PageTitle>Schedule</PageTitle>


<MudContainer MaxWidth="MaxWidth.Medium" Class="pa-4">
    <MudPaper Class="rounded-lg pa-2" Height="100" Elevation="6">
        <MudTimeline TimelinePosition="TimelinePosition.Start" DisableModifiers="false">
            @if (RaceTable is not null)
            {
                @foreach (var race in RaceTable.Races)
                {
                    <TimelineCard Race="race"/>
                }
            }
        </MudTimeline>
    </MudPaper>
</MudContainer>


